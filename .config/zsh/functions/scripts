# List custom scripts in the path
readonly SCRIPTS_DIR="$HOME/.local/bin/scripts/"

echo "Custom scripts in $SCRIPTS_DIR"
find "$SCRIPTS_DIR" -maxdepth 1 -type f -perm +111 -print0 | sort -z | while IFS= read -r -d '' file; do
    echo "  ${file:t}"
done
